* reset app version which is v0.9.0
* research "Advanced Settings" in Configuration
* research "Disable Animations"" in Configuration
* changing memory allocaton doesn't actually do anything though the UI works
* can we keep the same port number for noVNC when we restart
* fix setup lable: "IndosMemory }}ion"
* fix setup waiting message: it won't take up to an hour.